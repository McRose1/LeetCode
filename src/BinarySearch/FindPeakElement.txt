从一个数组里选出一个 peak（大于它左边和右边的元素），存在多个 peak 只需输出一个即可

要求 logarithmic complexity 
那么就说明需要 binary search

但是二分法的前提是需要 sorted array 

没想出来二分法的做法...

LC 官方：
这题有一个很重要的前提：nums[i] != nums[i+1]

最暴力的解法就是判断当前数字是否大于后面一个数字，非常巧妙

二分法：
其实思路很简单啊，只要比较中值和它后面一个值的大小
如果中值大于后面的值，说明现在降序，那么最高点会出现在左半边，反之，右半边
